# E-commerce Mister Print
Plugin para Wordpress de integração com a API do e-commerce Mister Print.

## Instalação
Para instalar o plugin execute os seguintes comandos:

`composer install`

`composer dump -o`

`npm install`

`npm run dev` ou `npm run production`
